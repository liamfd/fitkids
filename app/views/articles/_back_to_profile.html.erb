<% if user_signed_in? %>
	<% if current_user.type == "Child" %>
		<%= link_to "Back", child_profile_users_path(:id => current_user.id)%>
	<% elsif current_user.type == "Watcher"%>
		<%= link_to "Back", watcher_profile_users_path(:id => current_user.id)%> 
	<% end %>
<% end %>