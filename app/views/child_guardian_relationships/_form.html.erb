<%= form_for(@child_guardian_relationship) do |f| %>
  <% if @child_guardian_relationship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@child_guardian_relationship.errors.count, "error") %> prohibited this child_guardian_relationship from being saved:</h2>

      <ul>
      <% @child_guardian_relationship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
   <p>You are: 
    <%=@watchers.find_by_id(@this_watcher_id).name%></p>

    <%# f.collection_select( :watcher_id, @watchers, :id, :name, :prompt => true) %>
  </div>

  <div class="field">
    <%= f.label :relation %><br />
    <%= f.text_field :relation %>
  </div>
  <div class="field">
    <%= f.label :bonus %><br />
    <%= f.text_field :bonus %>
  </div>
  <div class="field">
    <%= f.label :child %><br />
    <%= f.collection_select :child_id, @children, :id, :name, :prompt => true %>
  </div>
  <div class="field">
    <%= f.hidden_field :watcher_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
