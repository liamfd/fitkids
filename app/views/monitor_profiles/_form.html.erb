<%= form_for(@monitor_profile) do |f| %>
  <% if @monitor_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@monitor_profile.errors.count, "error") %> prohibited this monitor_profile from being saved:</h2>

      <ul>
      <% @monitor_profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :bio %><br />
    <%= f.text_area :bio %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
