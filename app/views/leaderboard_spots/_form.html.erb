<%= form_for(@leaderboard_spot) do |f| %>
  <% if @leaderboard_spot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leaderboard_spot.errors.count, "error") %> prohibited this leaderboard_spot from being saved:</h2>

      <ul>
      <% @leaderboard_spot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :place %><br />
    <%= f.text_field :place %>
  </div>
  <div class="field">
    <%= f.label :leaderboard %><br />
    <%= f.text_field :leaderboard %>
  </div>
  <div class="field">
    <%= f.label :child %><br />
    <%= f.text_field :child %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
