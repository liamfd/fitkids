<h1>Listing monitor_child_relationships</h1>

<table>
  <tr>
    <th>Relation</th>
    <th>Bonus</th>
    <th>Child</th>
    <th>Monitor</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @monitor_child_relationships.each do |monitor_child_relationship| %>
  <tr>
    <td><%= monitor_child_relationship.relation %></td>
    <td><%= monitor_child_relationship.bonus %></td>
    <td><%= monitor_child_relationship.child %></td>
    <td><%= monitor_child_relationship.monitor %></td>
    <td><%= link_to 'Show', monitor_child_relationship %></td>
    <td><%= link_to 'Edit', edit_monitor_child_relationship_path(monitor_child_relationship) %></td>
    <td><%= link_to 'Destroy', monitor_child_relationship, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Monitor child relationship', new_monitor_child_relationship_path %>
